[build]
  command = "npm run build"
  publish = "build/client"
  functions = "netlify/functions"

# Route all requests through SSR function; static assets are handled by the handler
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200
